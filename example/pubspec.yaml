name: metadata_web_example
description: Demonstrates how to use metadata_web for extracting metadata on the web.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  metadata_web:
    path: ../
  metadata_core: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
